name: Deploy

on:
  workflow_call:

env:
  IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.PROJECT_NAME }}
  CONTAINER: quiz

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          script: |
            sudo docker rmi $IMAGE
            sudo docker pull $IMAGE
            sudo docker stop $CONTAINER
            sudo docker run --rm -d --name=$CONTAINER -p 80:80 $IMAGE
